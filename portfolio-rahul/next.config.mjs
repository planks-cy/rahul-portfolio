/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Swap in your own remote/CDN domains here when you replace placeholder images.
    remotePatterns: [],
  },
};

export default nextConfig;
